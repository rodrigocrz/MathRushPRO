<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mathrushpro">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MathRushPRO">
        <activity android:name=".MainActivity2_Nivel6" android:configChanges="orientation|screenSize"></activity>
        <activity android:name=".MainActivity2_Nivel5" android:configChanges="orientation|screenSize"/>
        <activity android:name=".MainActivity2_Nivel4" android:configChanges="orientation|screenSize"/>
        <activity android:name=".MainActivity2_Nivel3" android:configChanges="orientation|screenSize"/>
        <activity android:name=".MainActivity2_Nivel2" android:configChanges="orientation|screenSize"/>
        <activity android:name=".MainActivity2_Nivel1" android:configChanges="orientation|screenSize"/>
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/app_name"
            android:theme="@style/Theme.MathRushPRO.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>